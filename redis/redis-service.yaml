apiVersion: v1
kind: Service
metadata:
  name: redis-stack
  namespace: agrobd
  labels:
    app: redis-stack
spec:
  type: ClusterIP
  selector:
    app: redis-stack
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
  - name: redis-ui
    port: 8001
    targetPort: 8001